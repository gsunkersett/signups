{
  "name": "vanilla-basic",
  "version": "1.0.0",
  "scripts": {
    "start": "sirv ./src public --cors --single --no-clear --port 7071"
  },
  "dependencies": {
    "sirv": "^3.0.1"
  },
  "devDependencies": {
    "sirv-cli": "^3.0.1"
  }
}
